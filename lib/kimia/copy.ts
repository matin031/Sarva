/* ═══════════════════════════════════════════════════════════════════════════
   متنِ بازخوردها — یک جا، تا عوض کردنشان یک خط باشد و نه جست‌وجو در کد.
   ═══════════════════════════════════════════════════════════════════════════

   ⚠️ **هیچ متنی نمی‌گوید کدام رکن غلط بوده.** این یک قاعدهٔ آموزشی است و
   نه سلیقه: اگر بازی جای خطا را نشان دهد، بازیکن به‌جای تقطیع کردن،
   تک‌تک جایگاه‌ها را عوض می‌کند تا سبز شود. سرور هم عمداً `errorType`
   می‌دهد و نه شمارهٔ جایگاه.

   ⚠️ راهنماییِ سرور (`verdict.hint`) وقتی هست، به همین متنِ عمومی ترجیح
   داده می‌شود: آن راهنمایی از خودِ بانکِ پرسش‌ها می‌آید و دقیق‌تر است.
   ═══════════════════════════════════════════════════════════════════════════ */

export const KIMIA_COPY = {
  /** پاسخِ درست. `meter` اگر بیاید، نامِ وزن است. */
  correct: (meter?: string | null) =>
    meter ? `وزن درست است — ${meter}` : "وزن درست است.",

  /** پاسخِ غلط — بدونِ اشاره به جایگاهِ خطا. */
  wrong: "ترتیبِ ارکان با ریتم نمی‌خواند.",

  /** بیت هنوز نیامده. */
  loading: "در حالِ آوردنِ بیت…",

  /** وسطِ ترکیب. */
  mixing: "در حالِ ترکیب…",

  /** منتظرِ سرور، بعد از پایانِ انیمیشن. */
  analyzing: "در حالِ سنجش…",

  /** چند جایگاه خالی مانده. */
  remaining: (count: string) => `${count} جایگاهِ دیگر مانده`,

  /** همه پر است و آماده. */
  ready: "ترکیب آمادهٔ آزمایش است",

  /** بعد از پاسخِ غلط، تا وقتی چیدمان عوض نشده. */
  wrongIdle: "یک جایگاه را عوض کن و دوباره آزمایش کن.",

  /** دکمهٔ اصلی. */
  cta: {
    mix: "آزمایش ترکیب",
    retry: "فرستادنِ دوباره",
    next: "بیتِ بعدی",
    finish: "پایانِ نشست",
  },
} as const;

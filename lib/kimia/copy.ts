/* ═══════════════════════════════════════════════════════════════════════════
   متن بازخوردها — یک جا، تا عوض کردنشان یک خط باشد و نه جست‌وجو در کد.
   ═══════════════════════════════════════════════════════════════════════════

   ⚠️ **هیچ متنی نمی‌گوید کدام رکن غلط بوده.** این یک قاعدهٔ آموزشی است و
   نه سلیقه: اگر بازی جای خطا را نشان دهد، بازیکن به‌جای تقطیع کردن،
   تک‌تک جایگاه‌ها را عوض می‌کند تا سبز شود. سرور هم عمداً `errorType`
   می‌دهد و نه شمارهٔ جایگاه.

   ⚠️ راهنمایی سرور (`verdict.hint`) وقتی هست، به همین متن عمومی ترجیح
   داده می‌شود: آن راهنمایی از خود بانک پرسش‌ها می‌آید و دقیق‌تر است.

   ⚠️ و لحن: کوتاه، بدون کسرهٔ اضافه، بدون خط تیره وسط جمله. متن رابط
   باید مثل بقیهٔ سایت خوانده شود و نه مثل توضیح یک ماشین. (نسخهٔ قبلی
   «در حالِ آوردنِ بیت…» و «وزن درست است — رمل» بود؛ هر دو تلِ
   ماشین‌نویسی داشتند.)
   ═══════════════════════════════════════════════════════════════════════════ */

export const KIMIA_COPY = {
  /** پاسخ درست. `meter` اگر بیاید، نام وزن است. */
  correct: (meter?: string | null) => (meter ? `درست است. ${meter}` : "درست است."),

  /** پاسخ غلط — بدون اشاره به جایگاه خطا. */
  wrong: "ترتیب ارکان با ریتم نمی‌خواند.",

  /** بیت هنوز نیامده. */
  loading: "بیت در راه است…",

  /** وسط ترکیب. */
  mixing: "در حال ترکیب…",

  /** منتظر سرور، بعد از پایان انیمیشن. */
  analyzing: "در حال سنجش…",

  /** چند جایگاه خالی مانده. */
  remaining: (count: string) => `${count} جایگاه دیگر مانده`,

  /** همه پر است و آماده. */
  ready: "ترکیب آمادهٔ آزمایش است",

  /** بعد از پاسخ غلط، تا وقتی چیدمان عوض نشده. */
  wrongIdle: "یک جایگاه را عوض کن و دوباره آزمایش کن.",

  /* ─────────────────────── سه تلاش و نمایش پاسخ ────────────────────── */

  /**
   * شمارندهٔ تلاش.
   *
   * ⚠️ متن کامل است و نه فقط عدد، چون تنها چیزی است که صفحه‌خوان
   * می‌خواند: نقطه‌ها برای او وجود ندارند.
   */
  attemptsLeft: (n: string) => `${n} تلاش باقی‌مانده`,
  lastAttempt: "آخرین تلاش",

  /** دکمهٔ ثانویه، بعد از اولین غلط. */
  reveal: {
    ask: "نمایش پاسخ",
    confirm: "مطمئنی؟",
    /** وقتی خود بازیکن پاسخ را باز کرد. */
    done: "پاسخ درست:",
  },

  /** تلاش‌ها تمام شد. */
  exhausted: "تلاش‌ها تمام شد. پاسخ درست را ببین.",

  /* ──────────────────────────── پشت کارت ───────────────────────────── */

  back: {
    /** بازیکن خودش ساخت. */
    solved: "درست چیدی",
    /** پاسخ نشان داده شد (تلاش‌ها تمام شد یا خودش خواست). */
    shown: "وزن درست",
    /** ردیف چیدمان خود بازیکن، زیر پاسخ درست. */
    yours: "چیدمان تو",
    /** خوانش‌های دیگر همین وزن. */
    alternate: "خوانش دیگر",
    /** برچسب نوار ارکان برای صفحه‌خوان. */
    barLabel: "ارکان وزن، از راست به چپ",
  },

  /** دکمهٔ اصلی. */
  cta: {
    mix: "آزمایش ترکیب",
    retry: "فرستادن دوباره",
    next: "بیت بعدی",
    finish: "پایان نشست",
  },
} as const;

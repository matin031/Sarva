/**
 * پرتو از این شیء رد می‌شود.
 *
 * ⚠️ چرا لازم است. در این صحنه *تنها* جعبه‌های نامرئیِ `BookHitTarget` حق
 * دارند رویدادِ اشاره‌گر بگیرند. هر چیزِ دیگری — دیوار، طاقچه، کف، خودِ
 * مشِ کتاب، شخصیت، ذرات — اگر در پرتوافکنی شرکت کند دو دردسر می‌سازد:
 *
 *   ۱. می‌تواند جلوی جعبهٔ لمس را بگیرد و کلیک را بخورد.
 *   ۲. `onPointerMissed` و `onPointerOver`ِ والدها را بی‌دلیل روشن می‌کند.
 *
 * درسی که در `components/UI/aruz-bridge/scene/AnswerHitTarget.tsx` گرفته
 * شده بود: آنجا لبه‌های `LineSegments` با آستانهٔ پیش‌فرضِ یک‌متری، کلیکِ
 * کاشیِ راست را به کاشیِ چپ می‌دادند. راهِ درست این است که هرچه تزئینی است
 * صراحتاً از پرتوافکنی بیرون بماند، نه اینکه امیدوار باشیم جلو نیاید.
 *
 * three این را روی هر `Object3D` می‌پذیرد.
 */
export const NO_RAYCAST = () => null;

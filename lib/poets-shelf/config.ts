/* ═══════════════════════════════════════════════════════════════════════════
   کوکِ «قفسهٔ شاعران» — زمان‌بندی و امتیاز، یک‌جا.
   ═══════════════════════════════════════════════════════════════════════════

   هر عددی که «حس» بازی را می‌سازد اینجاست و نه پخش‌شده در کامپوننت‌ها. دلیلش
   عملی است: کوک‌کردنِ ریتمِ یک بازی یعنی چند عدد را کنارِ هم دیدن و با هم
   عوض کردن، نه گشتنِ هفت فایل.

   ⚠️ زمان‌های انیمیشن *اینجا* منبعِ حقیقت‌اند و در کامپوننت تکرار نمی‌شوند.
   ماشینِ حالت و خودِ انیمیشن هر دو از همین می‌خوانند، پس امکان ندارد از هم
   جدا بیفتند — چیزی که وقتی هر طرف تایمرِ خودش را داشت، بارها پیش می‌آمد. */

export interface PoetsShelfConfig {
  /** چند کتاب در هر دور. تغییرش هیچ مختصاتی را نمی‌شکند. */
  optionCount: number;

  /** سرعتِ دویدن (متر بر ثانیه). با چرخهٔ دویدنِ میکسامو کوک شده. */
  runSpeed: number;
  /** شتاب و کندشوندگی، تا حرکت از صفر نپرد. */
  runAccel: number;
  /** سرعتِ چرخشِ بدن به سمتِ مقصد (رادیان بر ثانیه). */
  turnSpeed: number;
  /** نزدیک‌تر از این به مقصد، «رسیده» حساب می‌شود. */
  arriveEpsilon: number;

  /** مکثِ کوتاه پس از رسیدن، پیش از آنکه کتاب واکنش نشان دهد. */
  settleMs: number;
  /** پاسخِ درست: مدتِ جشنِ کتاب پیش از شروعِ جشنِ شخصیت. */
  cheerLeadMs: number;
  /** پاسخِ درست: مدتِ کلِ جشن (با کلیپِ Victory کوک شده). */
  successMs: number;
  /** پاسخِ نادرست: از شروعِ پرتابِ کتاب تا لحظهٔ برخورد. */
  slamMs: number;
  /** پاسخِ نادرست: طولِ کلیپِ زمین‌خوردن. */
  fallMs: number;
  /** پاسخِ نادرست: مکثِ کمدی پس از افتادن، پیش از بلندشدن. */
  comedyPauseMs: number;

  /** محوشدنِ کلیپ‌ها. */
  crossfadeMs: number;
  /** برگشت به خانه: مدتِ بلندشدن پیش از دویدن. */
  standUpMs: number;

  scoring: {
    correct: number;
    streakBonus: number;
    wrong: number;
  };
}

export const defaultPoetsShelfConfig: PoetsShelfConfig = {
  optionCount: 5,

  /* ⚠️ ۲٫۴ متر بر ثانیه دلخواه نیست: چرخهٔ `Running` میکسامو ۰٫۷ ثانیه است و
     در هر چرخه دو گام برداشته می‌شود. با طولِ گامِ حدوداً ۰٫۸۵ متر برای قدِ
     ۱٫۵۵، سرعتِ بدونِ سُرخوردنِ پا می‌شود ‎۲×۰٫۸۵ ÷ ۰٫۷ ≈ ۲٫۴۳‎. عددِ
     پایین‌تر یعنی پاها روی زمین می‌لغزند. */
  runSpeed: 2.4,
  runAccel: 9,
  turnSpeed: 9,
  arriveEpsilon: 0.04,

  settleMs: 200,
  cheerLeadMs: 260,
  successMs: 1700,
  /* از لحظهٔ کنده‌شدنِ کتاب از طاقچه تا کوبیده‌شدن روی سر. کوتاه است چون
     ضربه باید *ناگهانی* باشد؛ قوسِ کندتر شبیهِ شناورشدن می‌شد. */
  slamMs: 380,
  /** طولِ کلیپِ بریده‌شدهٔ `hit` — نگاه کنید به scripts/poets-shelf/build-clips.mjs */
  fallMs: 2133,
  comedyPauseMs: 620,

  crossfadeMs: 180,
  standUpMs: 520,

  scoring: { correct: 100, streakBonus: 20, wrong: 0 },
};

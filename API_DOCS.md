# API سروا

مرجع کامل endpoint های HTTP. همهٔ مسیرها زیر `/api/v1/` هستند، به‌جز دو مسیر
قدیمی‌تر که در انتها آمده‌اند.

---

## قراردادهای مشترک

### شکل پاسخ

هر پاسخ یکی از این دو شکل است — بدون استثنا:

```jsonc
// موفق
{ "ok": true, "data": { ... } }

// ناموفق
{ "ok": false, "errors": ["پیام فارسی", "پیام دوم"] }
```

`errors` همیشه آرایه است، حتی برای یک خطا. پیام‌ها فارسی و قابل نمایش مستقیم
به کاربرند؛ جزئیات فنی (متن خطای پستگرس، مسیر فایل) هرگز در پاسخ نمی‌آیند و
فقط به لاگ سرور می‌روند.

### احراز هویت

با دو کوکی `httpOnly` انجام می‌شود که مرورگر خودکار می‌فرستد. **هیچ هدر
`Authorization` ای وجود ندارد** و توکن هرگز به جاوااسکریپت کلاینت داده نمی‌شود.

| کوکی | عمر پیش‌فرض | نقش |
|---|---|---|
| `sarva_at` | ۱۵ دقیقه | JWT دسترسی. تأییدش فقط بررسی امضاست، بدون کوئری. |
| `sarva_rt` | ۳۰ روز | توکن تازه‌سازی. ردیفی در `sessions` دارد، پس قابل ابطال است. |

`proxy.ts` روی هر درخواست اجرا می‌شود و اگر توکن دسترسی منقضی شده باشد، با
`sarva_rt` بی‌صدا تازه‌اش می‌کند. یعنی کلاینت معمولاً هرگز لازم نیست
`/auth/refresh` را صدا بزند.

### کدهای وضعیت

| کد | معنی |
|---|---|
| `200` | موفق |
| `201` | ساخته شد |
| `400` | ورودی نامعتبر (اعتبارسنجی) |
| `401` | وارد نشده‌اید، یا سشن منقضی است |
| `403` | وارد شده‌اید ولی اجازه ندارید (یا حساب مسدود است) |
| `404` | پیدا نشد |
| `409` | تعارض (مثلاً ایمیل تکراری) |
| `429` | تعداد درخواست بیش از حد |
| `500` | خطای سرور |

### هدر `x-request-id`

هر پاسخ — موفق یا ناموفق — یک هدر `x-request-id` دارد که `proxy.ts` برای همان
درخواست ساخته است. یک uuid است و هیچ اطلاعاتی از کاربر یا سشن در آن نیست.

به چه درد می‌خورد: وقتی کاربری خطای `500` می‌گیرد، همان مقدار در
`/admin/activity` کنار خطا نشسته و روی همهٔ خطوطِ لاگ سرورِ آن درخواست هم
هست. پس با یک `grep` می‌شود کل داستان آن درخواست را دید.

مقداری که کلاینت خودش در این هدر بفرستد **نادیده گرفته و بازنویسی می‌شود**.

### ترتیب بررسی‌ها

هر endpoint به این ترتیب کار می‌کند: **احراز هویت → مجوز → محدودیت نرخ →
اعتبارسنجی → دیتابیس**.

یعنی یک درخواست بدون سشن `401` می‌گیرد حتی اگر بدنه‌اش هم نامعتبر باشد. عمدی
است: کسی که وارد نشده نباید بتواند با فرستادن بدنه‌های مختلف، قواعد
اعتبارسنجی را کشف کند.

---

## احراز هویت

### `POST /api/v1/auth/register`

ساخت حساب. **بلافاصله وارد می‌کند** و کوکی‌ها را ست می‌کند.

<details>
<summary>ورودی و خروجی</summary>

```jsonc
// درخواست
{
  "name": "علی رضایی",     // ۳ تا ۶۰ نویسه، فقط فارسی
  "email": "ali@example.com",
  "password": "Abcd1234",   // ۸ تا ۷۲ نویسه؛ فاصله مجاز است، رمزهای رایج رد می‌شوند
  "turnstileToken": "0.ab…" // فقط وقتی کپچا فعال باشد (TURNSTILE_SECRET_KEY)
}
```

```jsonc
// ۲۰۱ — کوکی‌های sarva_at و sarva_rt ست می‌شوند
{
  "ok": true,
  "data": {
    "user": {
      "id": "9f1c…",
      "email": "ali@example.com",
      "fullName": "علی رضایی",
      "role": "student",
      "emailVerified": false,
      "isBanned": false,
      "createdAt": "2026-08-09T10:20:30.500Z"
    }
  }
}
```

خطاها: `400` اعتبارسنجی · `409` ایمیل تکراری · `429` بیش از ۱۰ ثبت‌نام در ۱۵
دقیقه از یک IP
</details>

> **چرا ورود بلافاصله و نه بعد از تأیید ایمیل:** ایمیل ممکن است نرسد
> (فیلترینگ، اسپم، قطعی سرویس). قفل کردن حساب پشت ایمیلی که شاید هرگز نیاید
> یعنی دانش‌آموزی که ثبت‌نام کرده وارد سایت نمی‌شود.

### `POST /api/v1/auth/login`

```jsonc
// درخواست
{ "email": "ali@example.com", "password": "Abcd1234" }

// ۲۰۰ — کوکی‌ها ست می‌شوند
{ "ok": true, "data": { "user": { /* مثل بالا */ } } }
```

خطاها:
- `401` `["ایمیل یا رمز عبور اشتباه است."]` — **یک پیام واحد** برای «ایمیل
  وجود ندارد» و «رمز غلط»، تا صفحهٔ ورود به ابزار فهرست‌کردن کاربران تبدیل
  نشود. زمان پاسخ هم در هر دو حالت برابر است.
- `403` حساب مسدود · `429` بیش از ۸ تلاش برای یک ایمیل، یا ۴۰ از یک IP، در ۱۵ دقیقه

### `POST /api/v1/auth/logout`

بدون بدنه. سشن را در دیتابیس باطل و کوکی‌ها را پاک می‌کند. **همیشه `200`** —
حتی بدون سشن.

```jsonc
{ "ok": true, "data": { "signedOut": true } }
```

### `POST /api/v1/auth/refresh`

بدون بدنه. با `sarva_rt` یک `sarva_at` تازه می‌دهد.

```jsonc
// ۲۰۰
{ "ok": true, "data": { "user": { /* … */ } } }
// ۴۰۱ — کوکی‌ها پاک می‌شوند
{ "ok": false, "errors": ["سشن معتبر نیست. دوباره وارد شوید."] }
```

> خودِ `sarva_rt` چرخانده نمی‌شود. چرخاندنش در هر استفاده امن‌تر است ولی دو تبِ
> همزمان همدیگر را بیرون می‌اندازند.

### `GET /api/v1/auth/me`

```jsonc
// ۲۰۰ — برای مهمان هم ۲۰۰ است، نه ۴۰۱
{ "ok": true, "data": { "user": null } }
```

### `PATCH /api/v1/auth/profile` 🔒

```jsonc
{ "name": "علی" }   // ۳ تا ۱۲ نویسه، فقط فارسی
// ۲۰۰
{ "ok": true, "data": { "user": { /* … */ } } }
```

> نام روی سروده‌ها و دیدگاه‌های گذشته عوض **نمی‌شود** — آن‌ها نام را در لحظهٔ
> ارسال عکس‌برداری می‌کنند.

### `POST /api/v1/auth/change-password` 🔒

```jsonc
{ "currentPassword": "Abcd1234", "newPassword": "Xyz98765" }
// ۲۰۰ — همهٔ سشن‌های دیگر باطل، برای این دستگاه سشن تازه صادر می‌شود
{ "ok": true, "data": { "changed": true } }
```

خطاها: `400` رمز فعلی غلط / رمز جدید همان قبلی · `429` بیش از ۵ تلاش در ۱۵ دقیقه

### `POST /api/v1/auth/send-verification` 🔒

بدون بدنه. کد ۶ رقمی به ایمیلِ **خودِ کاربر در سشن** می‌فرستد.

```jsonc
// ۲۰۰
{ "ok": true, "data": { "sent": true, "expiresInMinutes": 10 } }
{ "ok": true, "data": { "alreadyVerified": true } }
```

خطاها: `429` کول‌داون ۶۰ ثانیه، یا سقف ۳ ارسال در ۱۵ دقیقه، یا ۱۰ در ساعت از یک IP

> ایمیل از بدنه گرفته نمی‌شود. endpoint قدیمی هر آدرسی را قبول می‌کرد، یعنی
> هرکسی می‌توانست با آن به هر آدرسی در دنیا ایمیل بفرستد.

### `POST /api/v1/auth/verify-email` 🔒

```jsonc
{ "code": "123456" }   // ارقام فارسی «۱۲۳۴۵۶» هم پذیرفته می‌شود
// ۲۰۰
{ "ok": true, "data": { "verified": true, "user": { /* … */ } } }
```

خطاها: `400` `["کد وارد شده اشتباه یا منقضی شده است."]` — یک پیام برای «کدی
صادر نشده»، «منقضی» و «غلط». بعد از ۵ تلاش ناموفق کد می‌سوزد.

### `POST /api/v1/auth/forgot-password`

```jsonc
{ "email": "ali@example.com" }
// همیشه ۲۰۰، حتی وقتی آن ایمیل حسابی ندارد
{ "ok": true, "data": { "sent": true } }
```

> پاسخ در همهٔ حالت‌ها یکی است — حتی وقتی محدودیت نرخ فعال شده یا ارسال ایمیل
> شکست خورده. هر تفاوتی می‌گفت کدام ایمیل‌ها در سایت حساب دارند.

### `POST /api/v1/auth/reset-password`

```jsonc
{ "token": "…توکن از لینک ایمیل…", "password": "Xyz98765" }
// ۲۰۰ — همهٔ سشن‌های قبلی باطل، سشن تازه صادر می‌شود
{ "ok": true, "data": { "user": { /* … */ } } }
```

خطاها: `400` توکن نامعتبر/منقضی/مصرف‌شده · `429`

---

## آزمون وزن شعر (عروض سماعی)

### `POST /api/v1/quiz/answer` 🔒

```jsonc
{
  "questionId": "…uuid…",
  "selectedOptionId": "…uuid…"
}
// ۲۰۰
{ "ok": true, "data": { "isCorrect": true } }
```

> **درستی پاسخ فرستاده نمی‌شود.** سرور از روی `question_options.is_correct`
> حسابش می‌کند و بررسی می‌کند گزینه واقعاً به همان سؤال تعلق دارد. نسخهٔ قبلی
> این مقدار را از مرورگر می‌گرفت، یعنی با کنسول DevTools می‌شد برای هر سؤالی
> «درست» ثبت کرد.

پاسخ دوباره به یک سؤال، بازنویسی است نه ردیف تازه.

خطاها: `400` گزینه به این سؤال تعلق ندارد

### `POST /api/v1/quiz/attempt` 🔒

ثبت یک دور کامل. تعداد پاسخ‌های درست را سرور حساب می‌کند.

```jsonc
{
  "answers": [
    { "questionId": "…uuid…", "selectedOptionId": "…uuid…" },
    { "questionId": "…uuid…", "selectedOptionId": null }   // بی‌پاسخ
  ]
}
// ۲۰۱
{ "ok": true, "data": { "attemptId": "…uuid…", "total": 2, "correct": 1 } }
```

تلاش و پاسخ‌هایش در یک تراکنش نوشته می‌شوند.

### `GET /api/v1/quiz/answered`

```jsonc
// ۲۰۰ — برای مهمان فهرست خالی، نه ۴۰۱
{ "ok": true, "data": { "questionIds": ["…uuid…", "…uuid…"] } }
```

---

## جاسوسِ نقش‌ها

### `POST /api/v1/jasoos/answer` 🔒

```jsonc
{
  "levelId": 3,
  "category": "مدرسه",
  "verseLine1": "…",
  "verseLine2": "…",
  "chosenRole": "نهاد",
  "correctRole": "مفعول"
}
// ۲۰۱
{ "ok": true, "data": { "saved": true } }
```

> `isCorrect` فرستاده نمی‌شود؛ سرور دو نقش را مقایسه می‌کند.

---

## پلِ وزن

### `GET /api/v1/aruz-bridge/questions`

**بدون احراز هویت** — پرسش‌ها محتوای عمومی‌اند، مثلِ واژه‌های واژه‌یاب.

بازی کلِ مخزن را یک بار می‌گیرد و خودش نمونه‌گیری می‌کند (تا پیش از شروعِ دور
بداند چند پرسشِ یکتا موجود است)، پس این مسیر معمولاً بدونِ پارامتر صدا زده
می‌شود.

| پارامتر | الزامی | مقادیر |
|---|---|---|
| `difficulty` | نه | ۱ (آسان) \| ۲ \| ۳ (سخت) — بدون آن، همهٔ سطح‌ها |
| `limit` | نه | عددِ مثبت؛ سقفِ سختِ سرور ۱۰۰۰ است |

سختی از *فاصلهٔ دو وزن* می‌آید و هنگامِ بارگذاریِ محتوا حساب می‌شود: یک هجا
اختلاف یعنی سطحِ ۳، دو هجا سطحِ ۲، بیشتر سطحِ ۱.

```jsonc
// GET /api/v1/aruz-bridge/questions?difficulty=3&limit=2
{
  "ok": true,
  "data": {
    "questions": [
      {
        "id": "…uuid…",
        "promptText": "کجا",
        "correctPattern": "فعل",
        "wrongPattern": "فع‌لن",
        "difficulty": 3
      }
    ]
  }
}
```

`explanation` و `audioUrl` فقط وقتی می‌آیند که مقدار داشته باشند. جای چپ و
راست را دیتابیس تعیین نمی‌کند؛ بازی خودش هنگامِ ساختنِ دور یک بار قرعه می‌زند.

## واژه‌یاب

### `GET /api/v1/vocab/words`

**بدون احراز هویت** — واژه‌ها محتوای عمومی‌اند.

| پارامتر | الزامی | مقادیر |
|---|---|---|
| `grade` | بله | `dahom` \| `yazdahom` \| `davazdahom` |
| `lesson` | نه | ۱ تا ۱۸ — بدون آن، همهٔ درس‌های آن پایه |

```jsonc
// GET /api/v1/vocab/words?grade=dahom&lesson=2
{
  "ok": true,
  "data": {
    "words": [
      { "id": "…uuid…", "word": "میاسا", "meaning": "دست نکش", "image": "https://…", "lesson": 2 }
    ]
  }
}
```

### `POST /api/v1/vocab/answer` 🔒

```jsonc
{
  "grade": "dahom",
  "lesson": 2,
  "word": "میاسا",
  "meaning": "دست نکش",
  "image": "https://…",
  "isCorrect": true
}
// ۲۰۱
{ "ok": true, "data": { "saved": true } }
```

> ⚠️ برخلاف کوییز، `isCorrect` اینجا از کلاینت پذیرفته می‌شود. فراموش نشده:
> در واژه‌یاب گزینه‌های نادرست در مرورگر ساخته می‌شوند و هیچ‌جا ثبت نمی‌شوند،
> پس سرور نمی‌داند چه چیزی نشان داده شده. بستن واقعی‌اش یعنی ساختن دور بازی در
> سرور. پیامدش محدود است: این جدول فقط تاریخچهٔ شخصیِ «کدام واژه را اشتباه
> زدم» است و هیچ نمره‌ای از آن در نمی‌آید.

---

## نشان‌شده‌ها

همه 🔒. یک مسیر، چهار عمل.

### `GET /api/v1/bookmarks`

| پارامتر | اثر |
|---|---|
| — | همهٔ نشان‌شده‌ها |
| `?area=aruz` | فقط یک حوزه |
| `?area=aruz&refId=x` | فقط بررسی وجود |

```jsonc
// حالت فهرست
{ "ok": true, "data": { "bookmarks": [
  { "id": "…", "area": "aruz", "refId": "…", "title": "…",
    "subtitle": null, "payload": {}, "note": null, "createdAt": "2026-…" }
] } }

// حالت بررسی وجود
{ "ok": true, "data": { "bookmarked": true, "id": "…uuid…" } }
```

`area` یکی از `aruz` | `vocab` | `exam` | `jasoos`.

### `POST /api/v1/bookmarks`

```jsonc
{ "area": "aruz", "refId": "…", "title": "…", "subtitle": "…", "payload": {} }
// ۲۰۱ — تکراری، بازنویسی است نه خطا
{ "ok": true, "data": { "id": "…uuid…", "bookmarked": true } }
```

### `DELETE /api/v1/bookmarks`

```jsonc
{ "id": "…uuid…" }
// یا برای toggle:
{ "area": "aruz", "refId": "…" }

// ۲۰۰ — حذفی که چیزی پیدا نکند هم موفق است
{ "ok": true, "data": { "bookmarked": false } }
```

### `PATCH /api/v1/bookmarks`

```jsonc
{ "id": "…uuid…", "note": "این را دوباره مرور کن" }
{ "ok": true, "data": { "saved": true } }
```

---

## محتوای سایت

### `GET /api/v1/site-content`

محتوای قابل‌ویرایشِ سایت: نوار اعلان و بخش حامیان. بدون سشن، بدون دادهٔ
خصوصی.

چرا وجود دارد: صفحه‌های سایت **ایستا** ساخته می‌شوند و یک کوئری دیتابیس در
layout همه‌شان را dynamic می‌کند (ضمن اینکه مرحلهٔ build داکر اصلاً دیتابیس
ندارد). این endpoint اجازه می‌دهد هر دو تکه در زمان اجرا خوانده شوند بی‌آنکه
صفحه‌ها ایستا بودنشان را از دست بدهند.

```json
{
  "ok": true,
  "data": {
    "announcement": {
      "id": "…", "version": "2026-03-04T09:12:44.108Z",
      "title": "قطعی برنامه‌ریزی‌شده",
      "body": "فردا از ساعت ۲ تا ۴ بامداد…",
      "tone": "warning",
      "linkUrl": "/guide", "linkLabel": "جزئیات",
      "dismissible": true
    },
    "supporters": {
      "enabled": true, "title": "…", "subtitle": "…",
      "ctaUrl": "…", "ctaLabel": "…",
      "items": [{ "id": "…", "name": "…", "tier": "gold", "message": null,
                   "amountLabel": null, "linkUrl": null, "avatarUrl": null }]
    }
  }
}
```

`announcement` وقتی هیچ اعلانِ فعالی در بازهٔ زمانی نباشد `null` است.

⚠️ `linkUrl` و `avatarUrl` قبل از خروج از سرور اعتبارسنجی می‌شوند: فقط
`http://`، `https://` و مسیرهای داخلی. یک `javascript:` که مدیر (یا کسی که
به حساب مدیر رسیده) وارد کرده باشد، به‌همراه برچسبش حذف می‌شود — این لینک
بالای *همهٔ* صفحه‌ها برای *همهٔ* بازدیدکننده‌ها دیده می‌شود و بدترین XSS ممکن
بود.

برخلاف بقیهٔ endpoint ها این یکی `no-store` نیست: پاسخش به هیچ کاربری بستگی
ندارد، پس ۳۰ ثانیه کش می‌شود تا هر ناوبری در سایت یک کوئری اضافه نزند.

---

## گزارشِ ایرادِ محتوا

### `POST /api/v1/reports`

هر جای سایت که چیزی برای گزارش‌کردن هست — سؤالِ عروض سماعی، بیت و بندِ
درسنامه، پرسشِ امتحان، و هر هشت بازی — یک دکمهٔ «گزارش مشکل» دارد. این
endpoint گزارشِ همان دکمه را می‌گیرد.

**بدونِ ورود هم کار می‌کند.** برخلاف نشان‌کردن که بی‌حساب جایی برای ذخیره
ندارد، گزارش را سرور نگه می‌دارد؛ و بیشترِ کسانی که به سؤالِ غلط برمی‌خورند
اصلاً وارد نشده‌اند. بستنِ در به روی آن‌ها یعنی همان گزارشی که بیشتر از همه
لازم است هرگز نمی‌آید. اگر کاربر وارد باشد، `user_id` هم ثبت می‌شود.

```jsonc
// درخواست
{
  "area": "quiz",            // یکی از ۱۱ بخش — lib/reports/constants.ts
  "reason": "wrong_answer",  // یکی از ۸ دلیل
  "targetId": "45593b51-…",  // شناسهٔ محتوا در همان بخش (اختیاری)
  "snapshot": "به ناف آهو اگر مشک خون شود چه عجب…",  // متنی که کاربر می‌بیند
  "targetRef": { "type": "audio-to-poem", "question_number": 1 },
  "note": "گزینهٔ علامت‌خورده با ریتمِ پخش‌شده نمی‌خواند"  // حداکثر ۵۰۰ نویسه
}

// پاسخ
{ "ok": true }
```

**بخش‌ها:** `quiz` · `exam` · `vocab` · `grammar_circuit` · `aruz_rapid` ·
`aruz_bridge` · `jasoos` · `ninja` · `pairs` · `doroos` · `other`

**دلیل‌ها:** `wrong_answer` · `wrong_content` · `typo` · `audio` · `image` ·
`duplicate` · `offensive` · `other` — و فهرستِ دلیل‌هایی که به کاربر نشان
داده می‌شود به بخش بستگی دارد (نشان دادنِ «مشکل صوت» در بازی‌ای که صوت ندارد
فقط گزارشِ بی‌ربط می‌سازد).

`snapshot` عمداً *متنِ* محتوا را نگه می‌دارد و نه فقط شناسه‌اش: مدیر باید
بتواند سؤال را بعداً — حتی اگر ویرایش یا حذف شده باشد — با نوشتنِ یک مصراع
پیدا کند.

⚠️ دلیلِ `other` بدونِ `note` رد می‌شود (`۴۲۲`): گزارشی که نه دلیلِ مشخصی
دارد و نه توضیحی، هیچ‌کس نمی‌تواند رویش کاری کند. پنجره هم دکمه را تا نوشتنِ
توضیح قفل نگه می‌دارد، ولی قانون سمتِ سرور است.

⚠️ گزارش‌های تکراری روی یک محتوا **خطا نمی‌دهند**، `ok` می‌گیرند و ساکت دور
ریخته می‌شوند. پیامِ خطا به کاربری که گزارشش را دومین‌بار می‌فرستد چیزی
نمی‌گوید جز اینکه چیزی خراب است.

| محدودیت | سقف | پنجره |
|---|---|---|
| به ازای IP | ۱۵ | ۱۵ دقیقه |
| به ازای یک محتوای مشخص | ۲ | ۲۴ ساعت |

**در لاگ چه می‌رود:** فقط `report.created` با بخش و دلیل. متنِ گزارش، یادداشتِ
کاربر و `snapshot` هرگز لاگ نمی‌شوند — همان قاعدهٔ همیشگی که متنِ کاربر در
operational log نمی‌آید. `targetRef` هم پیش از لاگ redact می‌شود.

مدیریتِ گزارش‌ها در `/admin/reports` است: فیلترِ وضعیت و بخش، جست‌وجو در متنِ
گزارش‌ها، و «یافتنِ محتوا» که با یک مصراع در *همهٔ* بانک‌های محتوا می‌گردد —
نه فقط در گزارش‌ها. هر دو جست‌وجو متن را پیش از مقایسه یکدست می‌کنند (اعراب،
نیم‌فاصله، `ی`/`ك` عربی، فاصله و نشانه‌گذاری)، پس یک بیت را همان‌طور که روی
صفحه دیده‌اید می‌شود تایپ کرد.

هر گزارش یک لینکِ «رفتن به همین مورد» دارد که سرور می‌سازد: نشانیِ خودِ آن
محتوا در پنل، همراه با محدوده‌ای که پنل لازم دارد (`?grade=…&lesson=…&focus=…`).
`focus` را همهٔ پنل‌ها می‌فهمند — یا فرمِ ویرایش را باز می‌کند یا همان ردیف را
برجسته می‌کند.

---

## مسیرهای مستقل (بدون دیتابیس)

این دو از دوران قبل مانده‌اند و قرارداد پاسخِ بالا را دنبال **نمی‌کنند**.

### `GET /api/random-beyt`

یک بیت تصادفی از گنجور، برای دکمهٔ «بیتِ تصادفی» در وزن‌یاب.

```jsonc
// ۲۰۰
{
  "poet": "حافظ", "book": "غزلیات", "poem": "غزل ۱",
  "couplet": { "first": "…", "second": "…" },
  "url": "https://ganjoor.net/…", "poemId": 2130
}
// ۵۰۲
{ "error": "بیتی از گنجور به دست نیامد. دوباره تلاش کن." }
```

### `GET /api/vocab-image?src=…`

تصویر واژه را از یک allowlist از هاست‌های GitHub می‌گیرد، واترمارک می‌زند و
به‌صورت WebP برمی‌گرداند. خروجی **تصویر است، نه JSON**. اگر واترمارک شکست
بخورد، به تصویر اصلی ریدایرکت می‌کند.

---

## چیزهایی که endpoint نیستند

بخش‌های زیر با **Server Action** کار می‌کنند، نه HTTP، چون فقط از کد سمت سرور
صدا زده می‌شوند و هرگز از مرورگر مستقیم فراخوانی نمی‌شوند:

| بخش | فایل |
|---|---|
| پنل مدیریت (آزمون، کوییز، واژگان، کاربران، کلاب، تنظیمات) | `lib/admin/*`, `lib/exam/admin-actions.ts`, `lib/quiz/admin-actions.ts`, `lib/club/admin-actions.ts` |
| سروا کلاب (ارسال سروده، دیدگاه، پسند، گزارش) | `lib/club/actions.ts` |
| تصحیح و ثبت کارنامهٔ امتحان نهایی | `app/exam/[examKey]/actions.ts` |
| خواندن‌های پنل کاربر | `lib/panel/queries.ts` |

همه‌شان با `requireAdmin()` یا `requireUser()` شروع می‌شوند — همان گیتی که این
endpoint ها استفاده می‌کنند.

---

## محدودیت‌های نرخ — خلاصه

| کار | سقف | پنجره | شمارش در |
|---|---|---|---|
| ورود (به ازای ایمیل) | ۸ | ۱۵ دقیقه | حافظه |
| ورود (به ازای IP) | ۴۰ | ۱۵ دقیقه | حافظه |
| ثبت‌نام (به ازای IP) | ۱۰ | ۱۵ دقیقه | حافظه |
| تغییر رمز | ۵ | ۱۵ دقیقه | حافظه |
| فراموشی رمز (ایمیل / IP) | ۳ / ۱۵ | ۱۵ دقیقه / ۱ ساعت | حافظه |
| ارسال کد تأیید | کول‌داون ۶۰ ثانیه · ۳ در ۱۵ دقیقه · ۱۰ در ساعت per-IP | | **دیتابیس** |
| تلاش برای یک کد | ۵، بعدش کد می‌سوزد | تا انقضا | **دیتابیس** |
| گزارشِ محتوا (به ازای IP) | ۱۵ | ۱۵ دقیقه | حافظه |
| گزارشِ محتوا (به ازای یک محتوا) | ۲ | ۲۴ ساعت | حافظه |

> شمارنده‌های حافظه‌ای با ری‌استارت صفر می‌شوند و با چند نسخهٔ همزمانِ اپ سهمیه
> چند برابر می‌شود. محدودیت‌های OTP عمداً در دیتابیس‌اند چون باید از ری‌استارت
> جان سالم به در ببرند — وگرنه هر ری‌استارت یعنی امکان ارسال انبوه ایمیل.
> اگر روزی چند نسخه اجرا شد، `lib/api/rate-limit.ts` باید به Redis یا شمارندهٔ
> دیتابیسی تبدیل شود.

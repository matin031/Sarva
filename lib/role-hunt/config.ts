/** تنظیماتِ «شکار نقش‌ها» در یک جا — هیچ عددِ جادویی داخلِ کامپوننت‌ها. */

export const ROLE_HUNT_CONFIG = {
  /** دورهای یک نشست. هشت‌تا حدود سه دقیقه است — کوتاه‌تر از آنکه خسته کند. */
  roundsPerSession: 8,

  /**
   * یک دورِ کاملِ مدار، بر حسبِ ثانیه.
   *
   * ⚠️ روی صفحه آزموده شد: زیرِ ۱۲ ثانیه، واژه‌ها «فرار» به نظر می‌رسند و
   * کلیک کردن روی گوشی آزاردهنده می‌شود؛ بالای ۲۴ ثانیه حرکت آن‌قدر کند است
   * که به چشم نمی‌آید و مدار مرده به نظر می‌رسد. ۱۷ ثانیه وسطِ همان بازه‌ای
   * است که هم زنده است و هم قابلِ نشانه‌گیری.
   */
  orbitSeconds: 17,

  /** مدتِ رمزگشاییِ نقش (`٭٭٭` → «قید»). */
  decoderMs: 620,
  /** فاصلهٔ دو گامِ رمزگشا. مدت تقسیم بر این، تعدادِ فریم‌ها را می‌دهد. */
  decoderStepMs: 55,

  /** مکثِ بعد از پاسخ، پیش از دورِ بعد. */
  feedbackMs: 900,

  /** سقفِ دورهایی که از سرور خواسته می‌شود (نگهبانِ سادهٔ کوئری). */
  maxRoundsPerRequest: 20,
} as const;

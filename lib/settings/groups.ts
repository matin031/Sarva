/**
 * گروه‌بندیِ صفحهٔ تنظیمات — و **تنها بخشی از تنظیمات که مرورگر هم می‌بیند.**
 *
 * ⚠️ چرا یک فایلِ جدا و نه کنارِ بقیه در `index.ts`:
 *
 * `lib/settings/index.ts` برای خواندنِ مقادیر به `lib/db` وصل است، و `lib/db`
 * هم `mysql2` را می‌آورد و هم `server-only` علامت خورده. یعنی هر چیزی که از
 * آن ماژول یک *مقدار* import کند، اگر کامپوننتِ کلاینت باشد، درایورِ MySQL را
 * به بستهٔ مرورگر می‌کشد.
 *
 * و این دقیقاً یک بار اتفاق افتاد: صفحهٔ تنظیماتِ پنل مدیریت
 * (`components/admin/SettingsPanel.tsx`) که `"use client"` است، برای رفعِ یک
 * باگِ دیگر `SETTING_GROUPS` را از `@/lib/settings` گرفت. تا آن لحظه فقط یک
 * `import type` بود که در کامپایل پاک می‌شود؛ با تبدیل شدن به importِ مقدار،
 * کلِ زنجیره وارد شد و `next build` با «Module not found: mysql2» شکست.
 *
 * ⚠️ نکتهٔ ظریفش این است که `tsc` و `eslint` هر دو ساکت بودند — این خطا فقط
 * در `next build` دیده می‌شود. پس هرچه هر دو طرف لازم دارند، باید اینجا
 * بماند: این فایل هیچ import ای ندارد و هرگز نباید داشته باشد.
 */

/** گروه‌های صفحهٔ تنظیمات. `SettingSpec.group` از همین می‌آید. */
export type SettingGroup = "mail" | "sms" | "home" | "plus";

/** برچسب فارسی هر گروه، برای عنوان بخش‌ها در صفحهٔ تنظیمات. */
export const SETTING_GROUPS: Record<SettingGroup, { title: string; description: string }> = {
  mail: {
    title: "ایمیل",
    description: "کد تأیید حساب و لینک بازیابی رمز از این آدرس فرستاده می‌شوند.",
  },
  sms: {
    title: "پیامک",
    description:
      "هنوز هیچ بخشی از سایت پیامک نمی‌فرستد. این تنظیمات برای وقتی است که پنل پیامک بخرید — با پر کردن آن‌ها، ورود با موبایل بدون هیچ تغییری در کد فعال می‌شود.",
  },
  plus: {
    title: "سروا پلاس",
    description:
      "کلیدِ اصلیِ پولی‌بودنِ سایت و تنظیماتِ پیرامونش. قیمت و مدتِ پلن‌ها اینجا نیست — آن‌ها در صفحهٔ «سروا پلاس» پنل مدیریت ساخته می‌شوند، چون هر تغییرِ قیمت باید یک نسخهٔ تازه باشد و سابقهٔ خریدهای قبلی را دست نزند.",
  },
  home: {
    title: "صفحهٔ اصلی",
    description:
      "بخش حامیان. خودِ فهرست حامیان در صفحهٔ «حامیان» اداره می‌شود؛ این‌جا فقط تعیین می‌کنید دیده بشود یا نه و با چه عنوانی.",
  },
};
